import 'package:flutter/material.dart';

var kColorScheme = ColorScheme.fromSeed(
  seedColor: const Color.fromRGBO(57, 99, 192, 1),
);

var kDarkColorScheme =
    ColorScheme.fromSeed(seedColor: const Color.fromRGBO(120, 228, 255, 1));
